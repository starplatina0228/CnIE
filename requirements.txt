# CnIE — 창고 로봇 디스패칭 D3QN 실험
#
# 검증 환경: Python 3.11, torch 2.11.0 (CPU 빌드), numpy 2.4.4, matplotlib 3.10.9
# 학습/추론 모두 CPU 전용입니다 (agents.py 가 torch.device("cpu") 로 고정).
# → CUDA/GPU 불필요. torch 는 CPU 휠만 설치하면 됩니다.
#
# stdlib 외 실제 의존성은 아래 3개뿐 (csv/random/os/heapq/dataclasses/argparse 등은 표준 라이브러리).

torch>=2.0
numpy>=1.26
matplotlib>=3.7
